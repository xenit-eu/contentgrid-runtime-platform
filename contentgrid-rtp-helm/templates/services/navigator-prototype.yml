{{ if .Values.navigatorPrototype.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: navigator-prototype
  labels:
    app.kubernetes.io/name: webapp
    app.kubernetes.io/component: navigator-prototype
    app.kubernetes.io/part-of: contentgrid-runtimeplatform
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: webapp
      app.kubernetes.io/component: navigator-prototype
  replicas: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: webapp
        app.kubernetes.io/component: navigator-prototype
        app.kubernetes.io/part-of: contentgrid-runtimeplatform
        app.kubernetes.io/managed-by: {{ .Release.Service }}
    spec:
      containers:
        - name: navigator-prototype
          image: {{ .Values.navigatorPrototype.image }}
          {{ if .Values.development -}}
          imagePullPolicy: Always
          {{- end }}
          {{- if or .Values.navigatorPrototype.resourceLimits .Values.navigatorPrototype.resourceRequests }}
          resources:
            {{- with .Values.navigatorPrototype.resourceLimits }}
            limits:
              cpu: {{ .cpu }}
              memory: {{ .memory }}
            {{- end }}
            {{- with .Values.navigatorPrototype.resourceRequests }}
            requests:
              cpu: {{ .cpu }}
              memory: {{ .memory }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: 'API_BASE_URL'
              value: {{ .Values.navigatorPrototype.appUrl }}
            - name: 'OIDC_AUTHORITY'
              value: {{ .Values.navigatorPrototype.keycloakUrl }}
            - name: 'OIDC_CLIENT_ID'
              value: {{ .Values.navigatorPrototype.clientId }}
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
      enableServiceLinks: false
      automountServiceAccountToken: false
{{ include "nodeSelection" . | indent 6}}
---
apiVersion: v1
kind: Service
metadata:
  name: navigator-prototype-service
  labels:
    app.kubernetes.io/name: webapp
    app.kubernetes.io/component: navigator-prototype
    app.kubernetes.io/part-of: contentgrid-runtimeplatform
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  selector:
      app.kubernetes.io/name: webapp
      app.kubernetes.io/component: navigator-prototype
  ports:
    - name: http
      port: 80
      targetPort: http
      appProtocol: http
{{ with .Values.navigatorPrototype.host }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: navigator-prototype
  annotations:
    {{- with $.Values.certificates.defaultIssuer }}
    cert-manager.io/cluster-issuer: {{ . }}
    {{- end }}
  labels:
    app.kubernetes.io/name: webapp
    app.kubernetes.io/component: navigator-prototype
    app.kubernetes.io/part-of: contentgrid-runtimeplatform
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
spec:
  ingressClassName: {{ $.Values.ingressClassName }}
  rules:
  - host: {{ . }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: navigator-prototype-service
            port:
              name: http
  tls:
  - secretName: navigator-prototype-tls
    hosts:
    - {{ . }}
{{ end }}
{{ end }}
