# This fake secretstore is deployed separately as fakesecretstore.json
secretStoreName: xenit-fakesecretstore-ethias

userapps:
  database:
    - ip: "10.106.3.165"
      port: "5432"

navigator:
  image: docker.xenit.eu/customer-ethias/cg-poc/frontend@sha256:06ecfe91cae364ccb3219cf8f4fbf2a4ac0e72bde63ddbac2c76fecac6438774
  pullSecret: docker.xenit.eu

pathfinder:
  disabled: true

surveyor:
  disabled: true

rabbitmq:
  enabled: false

keycloak:
  host: auth.contentgrid-poc.ethias.be

keycloakx:
  database:
    hostname: "10.106.3.165"
    port: "5432"
  replicas: 1 # we only have a single node, and nodeaffinity means the replicas won't be scheduled on the same one

certificates:
  issuers: []

tokenmonger:
  host: extensions.ethias.contentgrid.example

liaison:
  renditions:
    uriTemplate: https://renditions.ethias.contentgrid.example/renditions/get/pdf{?url}
  extract:
    baseUrl: https://extract.ethias.contentgrid.example/extract

ingressClassName: ''

gatewayIpRange: 10\.42\.\d{1,3}\.\d{1,3}
